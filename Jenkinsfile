pipeline {
    agent any
    stages {
        stage('Connected') {
            steps {
                echo "TrueCD pipeline connected for ledger-api — MVP 0 ✅"
                sh 'java -version'
                sh 'docker --version'
            }
        }
    }
}